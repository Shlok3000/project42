class Umbrella{
    constructor(x,y){
        var options={
            isStatic: true,
            restitution: 0.8
        }
     
    }

    walkingman(){
        umbrella.addImage(umbrellaimage);
        image(0, 0, this.width, this.height);
    }
}